export const signup = async(req, res,next) => {
    res.send("signup route is called");
}

export const login = (req, res) => {
    res.send("login route is called");
}

export const logout = (req, res) => {
    res.send("logout route is called");
}